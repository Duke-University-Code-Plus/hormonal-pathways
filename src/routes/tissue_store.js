import {  writable } from 'svelte/store';

// Initialize writable stores with default values
export const gamma1_tissue = writable(3);
export const gamma2_tissue = writable(6);
export const gamma3_tissue = writable(9);